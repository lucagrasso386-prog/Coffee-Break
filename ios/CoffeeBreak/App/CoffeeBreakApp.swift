import SwiftUI

@main
struct CoffeeBreakApp: App {
    var body: some Scene {
        WindowGroup {
            // Placeholder root view — real screens start with
            // 08-page-accueil.md. This just proves the app boots.
            Text("Coffee Break")
        }
    }
}
