import Foundation

/// The game's coin currency (gold coin, coffee bean engraved), per
/// 03-assets-de-jeu.md.
enum Currency {
    /// Matches the imageset name in Assets.xcassets/Currency.
    static let coinAssetName = "coin-cafe"
}
