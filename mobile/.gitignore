# Generated by `flutter create .` — see mobile/README.md. Not committed for
# the same reason ios/CoffeeBreak.xcodeproj wasn't: this container can't run
# the generator, so nothing here can be kept in sync/verified.
/android/
/ios/
/web/
/linux/
/macos/
/windows/

.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/
